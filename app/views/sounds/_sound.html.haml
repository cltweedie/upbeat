%a{ href: "#{ sound_path(sound) }" }
  %h2
    = sound.title
%p.text--blue.text--bold.text--med
  by #{sound.producer.email}
%p instrument: <span class="text--blue">#{ sound.instrument.name }</span>
%p category: <span class="text--blue">#{ sound.category.name }</span>

.playbutton{ id: "play_button_#{sound.id}" }
= image_tag("/uploads/sound/#{sound.id}/#{sound.file.path.split('/')[-1]}.png", width: "80%")

%p
  tags:
  %span.text--blue
    - sound.tags.each do |tag|
      = link_to tag.name, sounds_path(tag: tag.name)

:javascript
      soundManager.onready(function() {
        $('#play_button_#{sound.id}').click(function() {
          soundManager.createSound({
              id: 'sound_#{ sound.id }',
              url: '#{ sound.file }',
              onfinish: function() {
                $('#play_button_#{sound.id}').css('background-position', '0 0');
              }
          });
          soundManager.play('sound_#{ sound.id }');
          $('#play_button_#{sound.id}').css('background-position', '100px 0');
        });

    });
